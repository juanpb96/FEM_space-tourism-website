// TODO: Check whether keep this file or use the one in src/services/types.ts
export interface ImagesFormat {
  png: string;
  webp: string;
}

export interface ImagesOrientation {
  portrait: string;
  landscape: string;
}
